# ProteoScope_protein.structure
A Flask API that uses ESMFold to predict 3D protein structures from sequences, aligns them with native RCSB PDB structures, and calculates $C\alpha$ RMSD completely in-memory.
